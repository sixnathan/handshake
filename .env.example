# ── ElevenLabs (STT — Scribe v2) ────────────
ELEVENLABS_API_KEY=
ELEVENLABS_REGION=us
ELEVENLABS_LANGUAGE=en

# ── Stripe (Connect + platform transfers) ───
STRIPE_SECRET_KEY=sk_test_...
STRIPE_PLATFORM_ACCOUNT_ID=acct_...
# Demo: pre-created Stripe customer with saved test card (pm_card_visa)
# Setup: stripe customers create → stripe payment_methods attach pm_card_visa --customer cus_XXX
STRIPE_CUSTOMER_ID=cus_...

# ── LLM ──────────────────────────────────────
LLM_PROVIDER=openrouter
LLM_API_KEY=
LLM_MODEL=anthropic/claude-sonnet-4-20250514

# ── Trigger Detection ───────────────────────
TRIGGER_KEYWORD=chripbbbly
SMART_DETECTION_ENABLED=true

# ── Monzo (optional, read-only) ─────────────
MONZO_ACCESS_TOKEN=

# ── Server ──────────────────────────────────
PORT=3000
